import { SwitchButtonDirective } from './switch-button.directive';

describe('SwitchButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchButtonDirective();
    expect(directive).toBeTruthy();
  });
});
